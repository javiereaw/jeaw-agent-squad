# OS
.DS_Store
Thumbs.db
nul

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Node (if testing locally)
node_modules/

# Temp
*.tmp
*.bak

# External references (cloned for research, not part of project)
.external-refs/

# Local settings
.claude/settings.local.json

# Symlinked directories (avoid duplicates in git)
# .claude/skills/ and .claude/rules/ are symlinks to .agent/
# Only track the source (.agent/), not the symlinks
.claude/skills/
.claude/rules/
